---
- name: first play debug
  gather_facts: true
  hosts: all
  tasks:
    - name: say hello
      ansible.builtin.debug:
        msg: hello-word
    - name: send debug payload
      ansible.builtin.uri:
        url: https://debug-api.malo.tech
        method: POST
        body_format: json
        body: '{"msg": "Hi from {{ ansible_hostname }} via Ansible in Bootc", "host": "{{ ansible_fqdn }}", "managed_by": "Red Hat Edge Manager" }'
        status_code: 201
          

